Re-enable auto-generation with Firebase management

- Re-enabled generateProducts() and seedDatabase() functions
- Products now auto-generate on first load if Firebase is empty
- Switched from manual phone uploads to Firebase Console management
- Products can be edited directly in Firebase Console
- Maintains 10 products per category for manageable catalog
