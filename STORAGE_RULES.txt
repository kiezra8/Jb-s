rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // ✅ Allow everyone to SEE images (Public Read)
      allow read: if true;
      
      // 🔒 Only Admin can UPLOAD/DELETE (Protected Write)
      allow write: if request.auth != null 
                   && request.auth.token.email == 'israelezrakisakye@gmail.com';
    }
  }
}
